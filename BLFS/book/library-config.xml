<para>和大多数库一样，这里不需要做额外的配置。不过，库文件的路径如<filename class="directory">/opt/lib</filename> 或 <filename class="directory">/usr/local/lib</filename>应该添加到 <filename>/etc/ld.so.conf</filename> 文件中，这样 ldd 才能找到这个库。在确认了这一点后，要以 root 的身份运行 <command>/sbin/ldconfig</command>。</para>
